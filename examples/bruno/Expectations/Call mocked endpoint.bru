meta {
  name: Call the mocked endpoint (GET /hello)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/hello
  body: none
  auth: none
}

docs {
  Calls the endpoint mocked above — returns 200 'Hello World'.
}
