meta {
  name: Clear (by request matcher)
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/mockserver/clear
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "path": "/hello"
  }
}

docs {
  Clear expectations and recorded requests matching the body (or all when empty).
}
